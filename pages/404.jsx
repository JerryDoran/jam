import styled from "styled-components";

const NotFound = () => {
  return (
    <NotFoundContainer>
      <h3>Not found</h3>
    </NotFoundContainer>
  );
};

export default NotFound;

// Styles
const NotFoundContainer = styled.div``;
